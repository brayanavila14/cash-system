
let btn = document.getElementById('atras')
import usuario from "./modulo1";

if (usuario = 'administrador') {
    formulario.setAttribute('action','inicio-administrador.html');
} else {
    formulario.setAttribute('action','inicio-administrador.html');
}